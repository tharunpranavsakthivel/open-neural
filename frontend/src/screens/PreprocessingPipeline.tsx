/**
 * PreprocessingPipeline screen - Step 2: Visual preprocessing pipeline builder.
 *
 * Re-exports the PipelineBuilder screen for backward compatibility.
 * The PipelineBuilder component provides the full preprocessing pipeline
 * building functionality with drag-and-drop support.
 *
 * @module screens/PreprocessingPipeline
 */
export { PipelineBuilder as PreprocessingPipeline } from "./PipelineBuilder";

# OpenNeural Backend - Development Dependencies
# These are only needed for development, testing, and CI/CD
# Install with: pip install -r requirements-dev.txt
# Or with the main package: pip install -e .[dev]

# Testing framework
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0

# HTTP client for testing
httpx>=0.27.0

# Code quality
ruff>=0.3.0
mypy>=1.8.0

# Type stubs
# Add as needed for better type checking
# types-pyyaml
# types-requests

{
  "name": "@openneural/backend",
  "version": "0.1.0",
  "private": true,
  "description": "FastAPI ML service workspace for OpenNeural.",
  "scripts": {
    "dev": "../.venv/bin/python -m openneural_backend --host 127.0.0.1 --port 8000",
    "build": "../.venv/bin/python -m compileall openneural_backend",
    "test": "../.venv/bin/python -m unittest discover -s test"
  }
}
